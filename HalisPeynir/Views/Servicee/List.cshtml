@model List<Servicee>

@{
    ViewData["Title"] = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row page-titles">
    <div class="col-md-5 align-self-center">
        <h4 class="text-themecolor">Services</h4>
    </div>
    <div class="col-md-7 align-self-center text-right">
        <div class="d-flex justify-content-end align-items-center">
            <a asp-controller="Servicee" asp-action="Add" method="post" type="button" class="btn btn-info d-none d-lg-block m-l-15"><i class="fa fa-plus-circle"></i> Create New</a>
        </div>
    </div>
</div>
<!-- row -->
<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table full-color-table full-danger-table hover-table">
                        <thead>
                            <tr>
                                <th>Service ID</th>
                                <th>Service Name</th>
                                <th>Service Price</th>
                                <th>Others</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Servicee service in Model)
                            {
                                <tr>
                                    <td>@service.ServiceeID</td>
                                    <td>@service.Name</td>
                                    <td>@service.Price</td>
                                    <td>
                                        <a class="like" asp-controller="Servicee" asp-action="Edit" asp-route-id="@service.ServiceeID" title="Edit">
                                            <i class="fas fa-edit fa-lg"></i>
                                        </a>


                                        <a class="like" asp-controller="Servicee" asp-action="Info" asp-route-id="@service.ServiceeID" title="İnfo">
                                            <i class="fas fa-info-circle fa-lg"></i>
                                        </a>


                                        <a class="like" asp-controller="Servicee" asp-action="Delete" asp-route-id="@service.ServiceeID" title="Delete">
                                            <i class="fas fa-trash fa-lg"></i>
                                        </a>

                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>